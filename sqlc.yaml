version: "2"
sql:
  - engine: "sqlite"
    schema: "./db/migrations"
    queries: "./db/queries"
    gen:
      go:
        package: "db"
        out: "./db"
        overrides:
          - column: "commands.parameters"
            go_type: "encoding/json.RawMessage"
